// Arquivo gerado automaticamente por src/schema_generator.py
// Gerado em 2026-06-03T14:09:34
// Baseado em 2 registro(s) de productsWorkspaces

generator client {
  provider             = "prisma-client-py"
  recursive_type_depth = 5
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Product {
  id String @id
  name String
  createdAt DateTime
  isActive Boolean

  @@map("products")
}
