// Arquivo gerado automaticamente por src/schema_generator.py
// Gerado em 2026-06-06T14:31:29
// Baseado em 1230 registro(s) de productsWorkspaces

generator client {
  provider             = "prisma-client-py"
  recursive_type_depth = 5
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Product {
  id String @id
  name String
  isBundle Boolean
  createdAt DateTime
  variantsAttributes String?
  featuredImage String?
  status String
  isPrivate Boolean
  workspace_id Int
  tier Int?
  markup String?
  quantityModifier Int
  provider_id String
  price String?
  analyzis String?
  productsPrices Json
  productsVariants Json
  products_productsCategories Json
  products_productsTags Json
  description String
  descript_translator String
  category_id String? @db.VarChar(250)
  category_name String?

  @@map("products")
}

model Category {
  id   String @id @db.VarChar(250)
  name String @db.VarChar(250)

  @@map("Category")
}
